# -----------------------------------------------------------------------
# keber/qa-framework - Environment Variables Reference
# -----------------------------------------------------------------------
# Copy this file to .env and fill in real values.
# NEVER commit .env to source control.
#
# This app is front-end only: no login, no backend/API (confirmed
# 2026-08-26 via live exploration). Most of the framework's default
# env vars (QA_USER_*, QA_LOGIN_*, QA_API_URL, ADO_*) do not apply here and
# were intentionally left out. See qa/qa-framework.config.json and
# qa/memory/ for the exploration findings.
# -----------------------------------------------------------------------

# -----------------------------------------------------------------------
# Application Under Test
# -----------------------------------------------------------------------
QA_BASE_URL=https://unicornt-store.keber.cl

# -----------------------------------------------------------------------
# CI / Pipeline overrides
# -----------------------------------------------------------------------
# CI=true           # Set automatically by GitHub Actions
